<% __render__ = import("/bin/markdown") %>
<% html = import("/bin/html") %>

# Rectangle

This document provides a function `draw(a,b)` that draws a rectangle on the
screen.

<% draw = (a,b) ->  html("svg", 
    {viewBox=str('0 0 ',2*a,' ',2*b), stroke="red", fill="grey"}, 
    ["rect", {width=a, height=b}]) %>
